# Build workflow for refind-btrfs-snapshots
# - On version tags (v*): Create a release, publish to AUR
# - On main branch: Create a pre-release snapshot with dev version
# - On PRs: Build and test only
#
# Note: This workflow does NOT run on main branch pushes if the commit is
# also tagged. Tags take precedence - the tag-triggered run creates the release.

name: Build Release

on:
  push:
    branches: [main]
    tags: ["v*"]
  pull_request:
    branches: [main]
  workflow_dispatch:
    inputs:
      snapshot_retention:
        description: "Number of snapshot pre-releases to keep"
        required: false
        default: "1"
        type: string

# Cancel in-progress runs for the same ref
concurrency:
  group: ${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

env:
  SNAPSHOT_RETENTION: ${{ inputs.snapshot_retention || '1' }}

permissions:
  contents: write

jobs:
  # ─── Stage 1: Version computation ─────────────────────────────────────
  prepare:
    name: Prepare
    runs-on: ubuntu-latest
    outputs:
      version: ${{ steps.version.outputs.version }}
      version_tag: ${{ steps.version.outputs.version_tag }}
      is_release: ${{ steps.version.outputs.is_release }}
      is_prerelease: ${{ steps.version.outputs.is_prerelease }}
      should_skip: ${{ steps.skip.outputs.should_skip }}
    steps:
      - name: Checkout
        uses: actions/checkout@v5
        with:
          fetch-depth: 0

      - name: Extract version
        id: version
        uses: jmylchreest/gha-extract-git-version@v1

      - name: Check skip conditions
        id: skip
        run: |
          SHOULD_SKIP=false

          # For main branch: skip if commit already has a release tag
          if [[ "${{ github.ref }}" == refs/heads/main && "${{ steps.version.outputs.is_tagged }}" == "true" ]]; then
            echo "Commit already has release tag, skipping main branch build"
            SHOULD_SKIP=true
          fi

          echo "should_skip=${SHOULD_SKIP}" >> $GITHUB_OUTPUT

          echo "Version: ${{ steps.version.outputs.version }}"
          echo "Version Tag: ${{ steps.version.outputs.version_tag }}"
          echo "Is Release: ${{ steps.version.outputs.is_release }}"
          echo "Is Prerelease: ${{ steps.version.outputs.is_prerelease }}"
          echo "Should Skip: ${SHOULD_SKIP}"

  # ─── Stage 2: Test ────────────────────────────────────────────────────
  test:
    name: Test
    runs-on: ubuntu-latest
    needs: prepare
    if: needs.prepare.outputs.should_skip != 'true'
    steps:
      - name: Checkout
        uses: actions/checkout@v5

      - name: Set up Go
        uses: actions/setup-go@v6
        with:
          go-version: "1.26"
          cache: true

      - name: Verify dependencies
        run: go mod verify

      - name: Build
        run: go build -v ./...

      - name: Run go vet
        run: go vet ./...

      - name: Install staticcheck
        run: go install honnef.co/go/tools/cmd/staticcheck@latest

      - name: Run staticcheck
        run: staticcheck ./...

      - name: Run tests
        run: go test -race -coverprofile=coverage.out -covermode=atomic ./...

      - name: Upload coverage to Codecov
        uses: codecov/codecov-action@v6
        with:
          files: ./coverage.out
        continue-on-error: true

  # ─── Stage 3: Build (refind + bls × linux/amd64 + linux/arm64) ────────
  build:
    name: Build ${{ matrix.binary.name }} (linux/${{ matrix.goarch }})
    runs-on: ubuntu-latest
    needs: [prepare, test]
    if: needs.prepare.outputs.should_skip != 'true' && github.event_name == 'push'
    strategy:
      fail-fast: false
      matrix:
        goarch: [amd64, arm64]
        binary:
          - { name: refind-btrfs-snapshots, path: ./cmd/refind-btrfs-snapshots, config: configs/refind-btrfs-snapshots.yaml, systemd: true,  man: true  }
          - { name: bls-btrfs-snapshots,    path: ./cmd/bls-btrfs-snapshots,    config: configs/bls-btrfs-snapshots.yaml,    systemd: true,  man: true  }
          - { name: uki-btrfs-snapshots,    path: ./cmd/uki-btrfs-snapshots,    config: configs/uki-btrfs-snapshots.yaml,    systemd: true,  man: true  }
          - { name: peseal,                 path: ./cmd/peseal,                 config: configs/peseal.yaml,                 systemd: true,  man: true  }
          - { name: kernel-spy,             path: ./cmd/kernel-spy,             config: "",                                  systemd: false, man: false }

    steps:
      - name: Checkout
        uses: actions/checkout@v5

      - name: Set up Go
        uses: actions/setup-go@v6
        with:
          go-version: "1.26"
          cache: true

      - name: Build binary
        env:
          CGO_ENABLED: 0
          GOOS: linux
          GOARCH: ${{ matrix.goarch }}
        run: |
          VERSION="${{ needs.prepare.outputs.version }}"
          COMMIT="${{ github.sha }}"
          DATE="$(date -u +%Y-%m-%dT%H:%M:%SZ)"

          go build -ldflags "-s -w \
            -X 'github.com/jmylchreest/refind-btrfs-snapshots/internal/version.Version=${VERSION}' \
            -X 'github.com/jmylchreest/refind-btrfs-snapshots/internal/version.Commit=${COMMIT}' \
            -X 'github.com/jmylchreest/refind-btrfs-snapshots/internal/version.BuildTime=${DATE}'" \
            -o "dist/${{ matrix.binary.name }}" ${{ matrix.binary.path }}

      - name: Prepare distribution
        run: |
          mkdir -p dist/assets
          BIN=${{ matrix.binary.name }}
          ARCH=${{ matrix.goarch }}
          VER=${{ needs.prepare.outputs.version }}

          # Standalone binary (consumed by the refind -bin AUR package; published
          # alongside the tarball for direct download for all binaries).
          cp dist/$BIN dist/assets/${BIN}-linux-${ARCH}

          # Tarball: binary + per-binary config + LICENSE, plus systemd units
          # for binaries that ship them. Built via a staging dir so the archive
          # layout is predictable and unrelated configs aren't bundled.
          STAGE="dist/stage-${BIN}-${ARCH}"
          mkdir -p "$STAGE"
          cp dist/$BIN "$STAGE/"
          cp LICENSE "$STAGE/"
          if [[ -n "${{ matrix.binary.config }}" ]]; then
            mkdir -p "$STAGE/configs"
            cp ${{ matrix.binary.config }} "$STAGE/configs/"
          fi
          if [[ "${{ matrix.binary.systemd }}" == "true" ]]; then
            mkdir -p "$STAGE/systemd"
            cp systemd/${BIN}.* "$STAGE/systemd/"
          fi
          if [[ "${{ matrix.binary.man }}" == "true" ]]; then
            mkdir -p "$STAGE/man"
            cp docs/man/${BIN}*.1 "$STAGE/man/"
          fi

          tar czf "dist/assets/${BIN}_${VER}_linux_${ARCH}.tar.gz" -C "$STAGE" .

      - name: Upload artifact
        uses: actions/upload-artifact@v6
        with:
          name: ${{ matrix.binary.name }}_linux_${{ matrix.goarch }}
          path: dist/assets/*
          if-no-files-found: error
          retention-days: 1

  # ─── Stage 3b: Build check for PRs (no artifacts) ──────────────────────
  build-check:
    name: Build Check ${{ matrix.binary }} (linux/${{ matrix.goarch }})
    runs-on: ubuntu-latest
    needs: [prepare, test]
    if: needs.prepare.outputs.should_skip != 'true' && github.event_name == 'pull_request'
    strategy:
      fail-fast: false
      matrix:
        goarch: [amd64, arm64]
        binary: [refind-btrfs-snapshots, bls-btrfs-snapshots, uki-btrfs-snapshots, peseal, kernel-spy]
    steps:
      - name: Checkout
        uses: actions/checkout@v5

      - name: Set up Go
        uses: actions/setup-go@v6
        with:
          go-version: "1.26"
          cache: true

      - name: Build binary
        env:
          CGO_ENABLED: 0
          GOOS: linux
          GOARCH: ${{ matrix.goarch }}
        run: |
          go build -ldflags "-s -w" -o /dev/null ./cmd/${{ matrix.binary }}

  # ─── Stage 4: Create Release ──────────────────────────────────────────
  release:
    name: Create Release
    runs-on: ubuntu-latest
    needs: [prepare, build]
    if: needs.prepare.outputs.should_skip != 'true' && github.event_name == 'push'
    steps:
      - name: Checkout
        uses: actions/checkout@v5
        with:
          fetch-depth: 0

      - name: Download all artifacts
        uses: actions/download-artifact@v7
        with:
          path: artifacts
          merge-multiple: false

      - name: Prepare release assets
        run: |
          mkdir -p release

          # Copy systemd units (for AUR direct download)
          cp systemd/refind-btrfs-snapshots.service release/
          cp systemd/refind-btrfs-snapshots.path release/

          # Move all build artifacts to release folder
          find artifacts -type f \( -name "*.tar.gz" -o -name "*-linux-amd64" -o -name "*-linux-arm64" \) -exec mv {} release/ \;

          echo "=== Release assets ==="
          ls -la release/

      - name: Generate checksums
        run: |
          cd release
          sha256sum * > checksums.txt
          echo "=== Checksums ==="
          cat checksums.txt

      - name: Generate changelog
        run: |
          # Find the last stable release tag (exclude dev/pre-release tags)
          PREV_TAG=$(git tag --list 'v[0-9]*.[0-9]*.[0-9]*' --sort=-v:refname \
            | grep -E '^v[0-9]+\.[0-9]+\.[0-9]+$' \
            | head -n1 || echo "")

          # For tagged releases, skip the current tag itself
          if [[ "${{ needs.prepare.outputs.is_release }}" == "true" && "$PREV_TAG" == "${{ needs.prepare.outputs.version_tag }}" ]]; then
            PREV_TAG=$(git tag --list 'v[0-9]*.[0-9]*.[0-9]*' --sort=-v:refname \
              | grep -E '^v[0-9]+\.[0-9]+\.[0-9]+$' \
              | sed -n '2p' || echo "")
          fi

          echo "Previous stable tag: ${PREV_TAG:-<none>}"

          if [[ "${{ needs.prepare.outputs.is_release }}" == "true" ]]; then
            if [ -n "$PREV_TAG" ]; then
              echo "## Changelog" > changelog.txt
              echo "" >> changelog.txt
              git log --pretty=format:"* %h %s" "$PREV_TAG"..HEAD | \
                grep -v "^* [a-f0-9]* Merge" >> changelog.txt || echo "* No notable changes" >> changelog.txt
            else
              echo "* Initial release" > changelog.txt
            fi
          else
            echo "> **Automated snapshot build from main branch**" > changelog.txt
            echo ">" >> changelog.txt
            echo "> This is an unstable development build. Use at your own risk." >> changelog.txt
            echo "> Install the latest stable version from the [Releases](../../releases) page instead." >> changelog.txt
            echo "" >> changelog.txt

            if [ -n "$PREV_TAG" ]; then
              echo "### Changes since ${PREV_TAG}" >> changelog.txt
              echo "" >> changelog.txt
              git log --pretty=format:"* %h %s" "$PREV_TAG"..HEAD | \
                grep -v "^* [a-f0-9]* Merge" >> changelog.txt || echo "* No notable changes" >> changelog.txt
            fi
          fi
          echo "=== Changelog ==="
          cat changelog.txt

      - name: Create release
        run: |
          if [[ "${{ needs.prepare.outputs.is_release }}" == "true" ]]; then
            gh release create "${{ needs.prepare.outputs.version_tag }}" \
              --title "${{ needs.prepare.outputs.version_tag }}" \
              --notes-file changelog.txt \
              release/*
          else
            gh release create "${{ needs.prepare.outputs.version_tag }}" \
              --title "Snapshot ${{ needs.prepare.outputs.version }}" \
              --prerelease \
              --notes-file changelog.txt \
              release/*
          fi
        env:
          GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}

      - name: Save checksums for publishing jobs
        uses: actions/upload-artifact@v6
        with:
          name: checksums
          path: release/checksums.txt
          retention-days: 1

      - name: Cleanup old snapshot releases
        if: needs.prepare.outputs.is_release != 'true'
        run: |
          RETENTION=${{ env.SNAPSHOT_RETENTION }}
          echo "Keeping ${RETENTION} most recent snapshot releases"

          # List snapshot prereleases (dev tags), newest first
          SNAPSHOTS=$(gh release list --limit 50 --json tagName,isPrerelease \
            --jq '.[] | select(.isPrerelease) | select(.tagName | test("^v[0-9]+\\.[0-9]+\\.[0-9]+-dev\\.")) | .tagName')

          COUNT=0
          for TAG in $SNAPSHOTS; do
            COUNT=$((COUNT + 1))
            if [ $COUNT -gt $RETENTION ]; then
              echo "Deleting old snapshot release: ${TAG}"
              gh release delete "${TAG}" --yes --cleanup-tag || true
            fi
          done

          echo "Cleanup complete. Kept ${RETENTION} most recent snapshots."
        env:
          GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
